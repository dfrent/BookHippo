<h3>Bookclubs</h3>

<% @book_clubs.each do |bookclub|  %>
  <h6> <%= bookclub.name %> </h6>
  <div class="book-club-name">
    <%= link_to(bookclub.name, book_club_path(bookclub[:id])) %>
    <br>
    <br>
  </div>
<% end %>
